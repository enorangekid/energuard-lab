project_id = "eukwfypbfqojbaihfqye"

[auth]
site_url = "http://localhost:3000"
additional_redirect_urls = []
enable_signup = false

[auth.mfa.totp]
enroll_enabled = true
verify_enabled = true

[auth.email]
enable_signup = true
double_confirm_changes = false
enable_confirmations = true
max_frequency = "1m0s"
otp_length = 8

[storage.vector]
enabled = false

[functions.naver-ad-report]
# Browser requests use an admin JWT; pg_cron uses x-energuard-cron-secret.
verify_jwt = false

[functions.blog-rank]
# Browser requests use an admin JWT; pg_cron uses x-energuard-cron-secret.
verify_jwt = false

[functions.item-draft-openai]
# Browser requests use an admin JWT. Authorization is enforced in the function.
verify_jwt = false

[functions.shopping-trend]
# Browser requests use an admin JWT; pg_cron uses x-energuard-cron-secret.
verify_jwt = false
